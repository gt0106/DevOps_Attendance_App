services:
  - type: web
    name: student-dashboard-app
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    autoDeployTrigger: commit
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_DB_NAME
        value: student_dashboard
      - key: MONGODB_URI
        sync: false
